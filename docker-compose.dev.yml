version: '3.8'

services:
  cctrs-web:
    volumes:
      - .:/app          # Mount entire project so code changes are instant
      - ./instance:/app/instance
    environment:
      - FLASK_ENV=development
      - FLASK_DEBUG=1
    command: python -m flask run --host=0.0.0.0 --port=5000 --reload
